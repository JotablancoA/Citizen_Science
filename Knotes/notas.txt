# instalar entorno virtual
pip install virtualenv
## crear entono virtual
virtualenv venv
# activar entorno virtual

.\venv\Scripts\activate

#instalar dependencias 
pip install -r packages.txt
pip install -r .\packages.txt

pip install datascience  # para cargar principales librerias de ciencia de datos
¡pip install plotly  # para mandar al terminal
%pip install adjustText

# Para cargar los archios en el repositorio
git add .
#Haz un commit de los archivos añadidos
git commit -m "mensaje del commit"
#Sube los cambios al repositorio remoto 
git push origin main


# 1. Navigate to app folder
cd app

# 2. Install dependencies
pip install -r requirements.txt

# 3. Launch dashboard
streamlit run app.py


cd "c:\Users\JoseAntonio.Blanco\OneDrive - Universidad de Castilla-La Mancha\Documentos\GitHub\Citizen_Science\app"; streamlit run app.py